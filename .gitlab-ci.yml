image: ubuntu
stages:
  - deploy
before_script:
  - 'which ssh-agent || ( apt-get update -y && apt-get install openssh-client git -y )'

  - eval $(ssh-agent -s)

  - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add -


  - mkdir -p ~/.ssh
  - chmod 700 ~/.ssh


  - ssh-keyscan 79.123.177.252 >> ~/.ssh/known_hosts
  - chmod 644 ~/.ssh/known_hosts



Deploy to test server:
  stage: deploy
  only:
    - triggers 
  script:
  - ssh dipp@79.123.177.252
  - cd nlp-tools-platform
  - git pull

